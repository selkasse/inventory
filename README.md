# An RPG-style inventory, made with React

## Move items with code

The user writes code in the in-browser editor (made possible with `react-ace`).

The code the user writes will move items into the inventory
